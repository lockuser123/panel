<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Login with Discord</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <div class="container">
    <h1>👋 Welcome to Your Control Panel</h1>
    <% if (user) { %>
      <p>You’re logged in as <%= user.username %>#<%= user.discriminator %>.</p>
      <a href="/dashboard" class="btn">Go to Dashboard</a>
      <a href="/logout" class="btn">Logout</a>
    <% } else { %>
      <a href="https://discord.com/api/oauth2/authorize?client_id=<%= process.env.CLIENT_ID %>&redirect_uri=<%= encodeURIComponent(process.env.REDIRECT_URI) %>&response_type=code&scope=identify" class="btn">Login with Discord</a>
    <% } %>
  </div>
</body>
</html>
